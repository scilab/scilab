 
getf SCI/util/testexamples.sci
 
reinit_for_test()
 
%U=mopen('/usr/local/lib/scilab/tests/Examples/other/firstnonsingleton_data.ref','r');
 
A = [1,2,3;4,5,6];
 
// Scilab max
 
M = max(A);
 
if load_ref('M') then   bugmes();quit;end,
 
 
// Matlab max emulation
 
M = max(A, firstnonsingleton(A));
 
if load_ref('M') then   bugmes();quit;end,
 
 
xdel_run(winsid());
 
 
mclose(%U);
 
