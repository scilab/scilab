 
getf SCI/util/testexamples.sci
 
reinit_for_test()
 
%U=mopen('SCI/tests/automatic_tests/loadwave_data.ref','rb');
 
// At first we create 0.5 seconds of sound parameters.
 
t = soundsec(0.5);
 
// Then we generate the sound: a two channels sound.
 
s = [sin(2 * %pi * 440 * t);sin(2 * %pi * 350 * t)];
 
savewave(TMPDIR + '/foo.wav', s);
Writing Wave file: Microsoft PCM format, 2 channels, 22050 samp/sec
        88200 byte/sec, 4 block align, 16 bits/samp
Finished writing Wave file, 44100 data bytes
 
s1 = loadwave(TMPDIR + '/foo.wav');
Reading Wave file: Microsoft PCM format, 2 channels, 22050 samp/sec
        88200 byte/sec, 4 block align, 16 bits/samp, 44100 data bytes
Input file: using sample rate 22050
	size shorts, style signed (2's complement), 2 channels
 
%ans = max(abs(s1 - s));
 
if load_ref('%ans') then   bugmes();quit;end,
 
 
xdel_run(winsid());
 
 
mclose(%U);
 
