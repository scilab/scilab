 
getf SCI/util/testexamples.sci
 
reinit_for_test()
 
%U=mopen('SCI/tests/graphical_tests/replot_data.ref','rb');
 
backupstyle = '?';
 
if load_ref('backupstyle') then   bugmes();quit;end,
 
 
if get('old_style') == 'off' then
  set('old_style', 'on');
  backupstyle = 'off';
  if load_ref('backupstyle') then   bugmes();quit;end,
end,
 
 
// under old graphics mode
 
x = (0:0.1:2 * %pi)';
 
%ans = plot2d(x, sin(x));
 
if load_ref('%ans') then   bugmes();quit;end,
 
 
%ans = replot([-1,-1,10,2]);
 
if load_ref('%ans') then   bugmes();quit;end,
 
 
 
// under new graphics mode
 
%ans = set('old_style', 'off');
 
if load_ref('%ans') then   bugmes();quit;end,
 
 
xdel_run(winsid());
 
%ans = plot();
Demo of plot
t=0:%pi/20:2*%pi;
subplot(211)
plot(t,sin(t),'ro-0.',t,cos(t),'cya+',t,abs(sin(t)),'--mo')
subplot(212)
plot([t ;t],[sin(t) ;cos(t)],'xdat',[1:2])
 
if load_ref('%ans') then   bugmes();quit;end,
 
// plot demo
 
f = gcf();
 
%ans = replot([-1,-1,10,2], f.children(1));
 
if load_ref('%ans') then   bugmes();quit;end,
 
// specify axes handle's value
 
%ans = replot([-3,-2,8,4], f.children(2));
 
if load_ref('%ans') then   bugmes();quit;end,
 
 
if backupstyle == 'off' then   set('old_style', backupstyle);end,
 
 
xdel_run(winsid());
 
 
mclose(%U);
 
