<?xml version="1.0" encoding="ISO-8859-1" standalone="no"?> 
<!DOCTYPE MAN SYSTEM "../../man.dtd">
<MAN>
  <LANGUAGE>eng</LANGUAGE>
  <TITLE>pvm_f772sci  </TITLE>
  <TYPE>Scilab function  </TYPE>
  <DATE>May 1998  </DATE>
  <SHORT_DESCRIPTION name="pvm_f772sci"> Convert a F77 complex into a complex scalar  </SHORT_DESCRIPTION>
  <CALLING_SEQUENCE>
  <CALLING_SEQUENCE_ITEM>[res] = pvm_f772sci(var)  </CALLING_SEQUENCE_ITEM>
  </CALLING_SEQUENCE>
  <PARAM>
 <PARAM_INDENT>
  <PARAM_ITEM>
  <PARAM_NAME>var : local scilab variable.  On  return, the  variable will  be  </PARAM_NAME>
  <PARAM_DESCRIPTION>
    overwritten with the result  of conversion operation.
  </PARAM_DESCRIPTION> 
  </PARAM_ITEM>
  <PARAM_ITEM>
  <PARAM_NAME>res  </PARAM_NAME>
  <PARAM_DESCRIPTION>
    : if the parameter var is not a variable, the result of the conversion  is returned in res.  
  </PARAM_DESCRIPTION> 
  </PARAM_ITEM>
 </PARAM_INDENT>
  </PARAM>
  <DESCRIPTION>
  <P>
    <VERB>pvm_f772sci</VERB>   converts all the  complex    of a scilab  variable
    represented  in teh F77  way into in  a  scilab representation. May be
    useful if  an   application  is receiving  data  from  a   non  scilab
    application (directly from a C or F77 program for example).
  </P>
  <P>
    Note  that  the parameter is passed  by  adress. It  means that if the
    parameter is a  variable, this variable will  be overwritten with  the
    result of conversion operation. On the other case, if the parameter is
    not a variable, the result will be returned in res.
  </P>
  <P>
    For example:
  </P>
  <VERBATIM><![CDATA[
-->a = [1+%i, 2+2*%i,3+3*%i]
 a  =
 
!   1. + i      2. + 2.i    3. + 3.i !
 
-->pvm_f772sci(a)
 
-->a
 a  =
 
!   1. + 2.i    3. + i      2. + 3.i !
 
-->b = pvm_f772sci([1+%i, 2+2*%i,3+3*%i])
 b  =
 
!   1. + 2.i    3. + i      2. + 3.i !
   ]]></VERBATIM>
  </DESCRIPTION>
  <SEE_ALSO>
    <SEE_ALSO_ITEM> <LINK>pvm_sci2f77</LINK> </SEE_ALSO_ITEM>
  </SEE_ALSO>
</MAN>
