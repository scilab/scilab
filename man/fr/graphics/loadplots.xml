<?xml version="1.0" encoding="ISO-8859-1" standalone="no"?> 
<!DOCTYPE MAN SYSTEM "../../man.dtd">
<MAN>
  <LANGUAGE>eng</LANGUAGE>
  <TITLE>loadplots  </TITLE>
  <TYPE>Scilab Function  </TYPE>
  <DATE>2001  </DATE>
  <SHORT_DESCRIPTION name="loadplots"> loads and formats saved plots  </SHORT_DESCRIPTION>
  <CALLING_SEQUENCE>
  <CALLING_SEQUENCE_ITEM>rec=loadplots(fil)  </CALLING_SEQUENCE_ITEM>
  </CALLING_SEQUENCE>
  <PARAM>
 <PARAM_INDENT>
  <PARAM_ITEM>
  <PARAM_NAME>fil  </PARAM_NAME>
  <PARAM_DESCRIPTION>
    : character string giving the path of the binary file containing the save of a graphic (see xsave) 
  </PARAM_DESCRIPTION> 
  </PARAM_ITEM>
  <PARAM_ITEM>
  <PARAM_NAME>rec  </PARAM_NAME>
  <PARAM_DESCRIPTION>
    : A data structure (see Description below)
  </PARAM_DESCRIPTION> 
  </PARAM_ITEM>
 </PARAM_INDENT>
  </PARAM>
  <DESCRIPTION>
  <P>
  </P>
  <P>
    Given a file generated by <VERB>xsave</VERB> of by the save menu in graphic window
    this function returns a data structure containing the context and plot definition.
  </P>
  <P>
    <VERB>rec</VERB> is a typed list with type <VERB>&quot;SPLOT&quot;</VERB>. Each of its
    <VERB>2:$</VERB> fields contains  a graphic instruction definition and its
    parameters. <VERB>rec(i)(1)</VERB> gives the fields names of the <VERB>i</VERB> th graphic
    instruction 
  </P>
  <P>
    <VERB>txt=string(rec)</VERB>  generates the Scilab instructions
    which reproduce the plot. To make the necessary functions known, load
    the <VERB>loadplots</VERB> function first. This can be done with the instruction 
  </P>
  <VERBATIM><![CDATA[
loadplots;
   ]]></VERBATIM>
  </DESCRIPTION>
  <EXAMPLE><![CDATA[

driver('Rec');xbasc();plot2d([0 1.5 4]) //make a plot
xsave(TMPDIR+'/foo.scg') //save it in a binary file

rec=loadplots(TMPDIR+'/foo.scg'); //get the associated data structure
//here rec(9) is the data structure associated with the plot2d instruction
rec(9).x //the x vector
rec(9).y //the y vector

string(rec) //the scilab instructions producing the same plot

mputl(string(rec),TMPDIR+'/foo.sce') //creates a script file
xbasc();exec(TMPDIR+'/foo.sce',-1) //execute it to re-create the plot

 ]]></EXAMPLE>
  <SEE_ALSO>
    <SEE_ALSO_ITEM> <LINK>xsave</LINK> </SEE_ALSO_ITEM>     <SEE_ALSO_ITEM> <LINK>xload</LINK> </SEE_ALSO_ITEM>
  </SEE_ALSO>
</MAN>
