<?xml version="1.0" encoding="ISO-8859-1" standalone="no"?> 
<!DOCTYPE MAN SYSTEM "../../manrev.dtd">
<MAN>
  <LANGUAGE>fr</LANGUAGE>
  <TITLE>execstr  </TITLE>
  <TYPE>Scilab Function  </TYPE>
  <DATE>April 1993  </DATE>
  <SHORT_DESCRIPTION name="execstr"> exécution d'instructions Scilab contenues dans des chaînes de caractères</SHORT_DESCRIPTION>
  <CALLING_SEQUENCE>
    <CALLING_SEQUENCE_ITEM>execstr(instr)  </CALLING_SEQUENCE_ITEM>
    <CALLING_SEQUENCE_ITEM>ierr=execstr(instr,'errcatch' [,msg])  </CALLING_SEQUENCE_ITEM>
  </CALLING_SEQUENCE>
  <PARAM>
    <PARAM_INDENT>
      <PARAM_ITEM>
	<PARAM_NAME>instr  </PARAM_NAME>
	<PARAM_DESCRIPTION>
	  <SP>: vecteur de chaînes de caractères</SP>
	</PARAM_DESCRIPTION> 
      </PARAM_ITEM>
      <PARAM_ITEM>
	<PARAM_NAME>ierr  </PARAM_NAME>
	<PARAM_DESCRIPTION>
	  <SP>: entier, 0 ou numéro de l'erreur</SP>
	</PARAM_DESCRIPTION> 
      </PARAM_ITEM>
      <PARAM_ITEM>
	<PARAM_NAME>msg  </PARAM_NAME>
	<PARAM_DESCRIPTION>
	  <SP>: chaîne de caractères (<VERB>'m'</VERB> ou <VERB>'n'</VERB>). La valeur
	    par défaut est <VERB>'n'</VERB>. </SP>
	</PARAM_DESCRIPTION> 
      </PARAM_ITEM>
    </PARAM_INDENT>
  </PARAM>
  <DESCRIPTION>
    <P>Exécute les instructions Scilab données dans l'argument
      <VERB>instr</VERB>. </P>
    <P>Note : les instructions ne doivent pas comporter de marques de suite (..). </P>
    <P> </P>
    <P>Si 'errcatch' n'est pas présent dans les arguments, les erreurs sont contrôlées comme d'habitude.</P>
    <P>Si 'errcatch' est présent dans les arguments, et qu'une erreur est
      rencontrée lors de l'évaluation d'un élément de <VERB>instr</VERB>,
      <VERB>execstr</VERB> ne produit aucun message d'erreur, mais stoppe
      l'exécution des instructions stockées dans <VERB>instr</VERB> (au point
      où l'erreur s'est produite), et renvoie dans <VERB>ierr</VERB> le numéro
      de l'erreur. Dans ce cas l'affichage du message d'erreur est contrôlé par
      l'option <VERB>msg</VERB> : </P>

    <DESCRIPTION_INDENT>
      <DESCRIPTION_ITEM label='&quot;m&quot;'> 
	<SP>: le message d'erreur est affiché et enregistré</SP>
      </DESCRIPTION_ITEM>
      <DESCRIPTION_ITEM  label='&quot;n&quot;'> 
	<SP>: le message d'erreur n'est pas affiché mais il est enregistré
	  (voir <VERB>lasterror</VERB>). Il s'agit du comportement par
	  défaut. </SP>
      </DESCRIPTION_ITEM>
    </DESCRIPTION_INDENT>
    <P> ierr=execstr(instr,'errcatch') peut gérer les erreurs de syntaxes. Ceci
      peut être utile pour évaluer les instructions obtenues par une requète à
      l'utilisateur (interface homme-machine).</P>
  </DESCRIPTION>
  <EXAMPLE><![CDATA[
execstr('a=1') // affecte a=1.

execstr('1+1') // ne fait rien de particulier (alors que evstr('1+1') renvoie 2)

execstr(['if %t then';
         '  a=1';
         '  b=a+1';
         'else'
         ' b=0'
         'end'])

// récupération des erreurs d'exécution
execstr('a=zzzzzzz','errcatch')
execstr('a=zzzzzzz','errcatch','m')

// récupération des erreurs de syntaxe
execstr('a=1?02','errcatch')
lasterror(%t)

execstr('a=[1 2 3)','errcatch')
lasterror(%t)

 ]]></EXAMPLE>
  <SEE_ALSO>
    <SEE_ALSO_ITEM>
      <LINK>try</LINK>
    </SEE_ALSO_ITEM>
    <SEE_ALSO_ITEM>
      <LINK>errclear</LINK>
    </SEE_ALSO_ITEM>
    <SEE_ALSO_ITEM>
      <LINK>iserror</LINK>
    </SEE_ALSO_ITEM>
    <SEE_ALSO_ITEM>
      <LINK>whereami</LINK>
    </SEE_ALSO_ITEM>
    <SEE_ALSO_ITEM>
      <LINK>execstr</LINK>
    </SEE_ALSO_ITEM>
  </SEE_ALSO>
</MAN>
