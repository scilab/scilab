<?xml version="1.0" encoding="UTF-8"?>
<!--
 * Scilab ( http://www.scilab.org/ ) - This file is part of Scilab
 * Copyright (C) 2008 - INRIA
 * 
 * This file must be used under the terms of the CeCILL.
 * This source file is licensed as described in the file COPYING, which
 * you should have received as part of this distribution.  The terms
 * are also available at    
 * http://www.cecill.info/licences/Licence_CeCILL_V2-en.txt
 *
 -->
<refentry version="5.0-subset Scilab" xml:id="sort" xml:lang="en"
          xmlns="http://docbook.org/ns/docbook"
          xmlns:xlink="http://www.w3.org/1999/xlink"
          xmlns:svg="http://www.w3.org/2000/svg"
          xmlns:ns5="http://www.w3.org/1999/xhtml"
          xmlns:mml="http://www.w3.org/1998/Math/MathML"
          xmlns:db="http://docbook.org/ns/docbook">
  <info>
    <pubdate>$LastChangedDate: 2008-04-10 16:45:48 +0200 (jeu., 10 avr. 2008)
    $</pubdate>
  </info>

  <refnamediv>
    <refname>sort</refname>

    <refpurpose>ordenamento decrescente (DEPRECATED see gsort)</refpurpose>
  </refnamediv>

  <refsynopsisdiv>
    <title>Seqüência de Chamamento</title>

    <synopsis>
      [s, [k]]=sort(v)
      [s, [k]]=sort(v,'r')
      [s, [k]]=sort(v,'c')
</synopsis>
  </refsynopsisdiv>

  <refsection>
    <title>Parâmetros</title>

    <variablelist>
      <varlistentry>
        <term>v</term>

        <listitem>
          <para>vetor esparso; vetor ou matriz de reais, complexos ou
          "strings" </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term>s</term>

        <listitem>
          <para>vetor esparso; vetor ou matriz de reais, complexos ou
          "strings" </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term>k</term>

        <listitem>
          <para>vetor ou matriz de inteiros</para>
        </listitem>
      </varlistentry>
    </variablelist>
  </refsection>

  <refsection>
    <title>Descrição</title>

    <para> sort will be removed in Scilab 5.3. see gsort </para>

    <para>A função <literal>sort</literal> está obsoleta. Ela pode ser
    substituída por <link linkend="gsort">gsort</link>.</para>

    <para><literal>s=sort(v)</literal> põe <literal>v</literal> em ordem
    decrescente. Se <literal>v</literal> é uma matriz, a ordenação é feita
    coluna a coluna, <literal>v</literal> sendo visto como o vetor empilhado
    <literal>v(:)</literal>. Se <literal>v</literal> é um string, a ordenação
    é crescente. <literal>[s,k]=sort(v)</literal> retorna ainda os índices de
    entradas de <literal>s</literal> em <literal>v</literal>, i.e.
    <literal>v(k(:)) </literal> é o vetor <literal>s</literal>.</para>

    <para><literal>s=sort(v,'r')</literal> põe as linhas de
    <literal>v</literal> em ordem decrescente i.e. cada coluna de
    <literal>s</literal> é obtida de cada coluna de <literal>v</literal> por
    reordenação decrescente. <literal>[s,k]=sort(v,'r')</literal> retorna
    ainda em cada coluna de <literal>k</literal> os índices tais que
    <literal>v(k(:,i),i)=s(:,i)</literal> para cada índice de coluna
    <literal>i</literal>.</para>

    <para><literal>s=sort(v,'c')</literal> põe as colunas de
    <literal>v</literal> em ordem decrescente i.e. cada linha de
    <literal>s</literal> é obtida de cada linha de <literal>v</literal> por
    reordenação decrescente. <literal>[s,k]=sort(v,'c')</literal> retorna
    ainda em cada linha de <literal>k</literal> os índices tais que
    <literal>v(i,k(i,:))=s(i,:)</literal> para cada índice de linha
    <literal>i</literal>.</para>

    <para>Matrizes ou vetores de complexos são ordenados de acordo com suas
    magnitudes. Ordenação por colunas/linhas não é implementada para matrizes
    complexas.</para>

    <para><literal>y=sort(A)</literal> é válido quando <literal>A</literal> é
    um vetor esparso. Ordenação de linhas/colunas não é implementado para
    matrizes esparsas.</para>

    <para>Limitação : se <literal>v</literal> inclui elementos %nan, a matriz
    não será ordenada. Por favor, use <link linkend="gsort">gsort</link> neste
    caso.</para>
  </refsection>

  <refsection>
    <title>Exemplos</title>

    <programlisting role="example"><![CDATA[ 
[s,p]=sort(rand(1,10));
//p  é uma permutação aleatória de 1:10
A=[1,2,5;3,4,2];
[Asorted,q]=sort(A);A(q(:))-Asorted(:)
v=1:10;
sort(v)
sort(v')
sort(v,'r')  //Não faz nada para vetores-linha
sort(v,'c')
 ]]></programlisting>
  </refsection>

  <refsection>
    <title>Ver Também</title>

    <simplelist type="inline">
      <member><link linkend="find">find</link></member>

      <member><link linkend="gsort">gsort</link></member>
    </simplelist>
  </refsection>
</refentry>
