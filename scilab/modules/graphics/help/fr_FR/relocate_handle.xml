<?xml version="1.0" encoding="ISO-8859-1" standalone="no"?>
<!DOCTYPE MAN SYSTEM "../../../../modules/helptools/help.dtd">
<MAN>
  <LANGUAGE>fr</LANGUAGE>

  <TITLE>relocate_handle</TITLE>

  <TYPE>Scilab Function</TYPE>

  <DATE>$LastChangedDate$</DATE>

  <SHORT_DESCRIPTION name="relocate_handle">Déplace un handle à &#160;
  l'intérieur de la hiérarchie graphique.</SHORT_DESCRIPTION>

  <CALLING_SEQUENCE>
    <CALLING_SEQUENCE_ITEM>relocate_handle( movedHandles, parent
    )</CALLING_SEQUENCE_ITEM>
  </CALLING_SEQUENCE>

  <PARAM>
    <PARAM_INDENT>
      <PARAM_ITEM>
        <PARAM_NAME>movedHandles</PARAM_NAME>

        <PARAM_DESCRIPTION>
          <SP>: Vecteur contenant les handles déplacés.</SP>
        </PARAM_DESCRIPTION>
      </PARAM_ITEM>

      <PARAM_ITEM>
        <PARAM_NAME>parent</PARAM_NAME>

        <PARAM_DESCRIPTION>
          <SP>: Nouveau parent des handles.</SP>
        </PARAM_DESCRIPTION>
      </PARAM_ITEM>
    </PARAM_INDENT>
  </PARAM>

  <DESCRIPTION>
    <P>La routine <BD>relocate_handle</BD> permet de déplacer des handles
    graphiques à &#160; partir de leurs emplacements actuels vers un autre dans
    la hierarchie graphique. Les entités déplacées sont relogées sous le
    handle <BD>parent</BD> spécifié.</P>

    <P>Comme tous les handles graphiques ne sont pas compatibles entre eux, il
    existe des restrictions quant à &#160; l'utilisation de la routine . Il est
    par exemple impossible de déplacer une handle axes comme enfant d'une
    polyline. Pour plus d'information à &#160; ce sujet, on peut se reporter
    à &#160; la page <LINK>graphics_entities</LINK>.</P>

    <P>Cette routine est particulièrement utile pour déplacer un objet d'un
    axe à &#160; un autre ou pour déplacer des axes entre fenêtre.</P>
  </DESCRIPTION>

  <EXAMPLE><![CDATA[x = 0:10 ;

// construction d'une première polyline
plot(x,x^2) ;
axes1 = gca() ;
poly1 = gce() ;

// puis d'une seconde
scf() ;
plot( x,x )  ;
axes2 = gca() ;
poly2 = gce()  ;
poly2bis = copy( poly2 ) ; // copie de la polyline pour la déplacer ensuite

// les deux polylines se retrouvent dans le même axe.
relocate_handle( poly2bis, axes1 ) ;

]]></EXAMPLE>

  <SEE_ALSO>
    <SEE_ALSO_ITEM>
      <LINK>graphics_entities</LINK>
    </SEE_ALSO_ITEM>

    <SEE_ALSO_ITEM>
      <LINK>copy</LINK>
    </SEE_ALSO_ITEM>

    <SEE_ALSO_ITEM>
      <LINK>delete</LINK>
    </SEE_ALSO_ITEM>

    <SEE_ALSO_ITEM>
      <LINK>swap_handles</LINK>
    </SEE_ALSO_ITEM>
  </SEE_ALSO>

  <AUTHOR>Jean-Baptiste Silvy</AUTHOR>
</MAN>