<?xml version="1.0" encoding="UTF-8"?>
<!--
 * Scilab ( http://www.scilab.org/ ) - This file is part of Scilab
 * Copyright (C) INRIA - Jean-Baptiste Silvy
 * 
 * This file must be used under the terms of the CeCILL.
 * This source file is licensed as described in the file COPYING, which
 * you should have received as part of this distribution.  The terms
 * are also available at    
 * http://www.cecill.info/licences/Licence_CeCILL_V2-en.txt
 *
 -->
<!DOCTYPE MAN SYSTEM "../../../../modules/helptools/help.dtd">
<MAN>
  <LANGUAGE>eng</LANGUAGE>

  <TITLE>relocate_handle</TITLE>

  <TYPE>Scilab Function</TYPE>

  <DATE>$LastChangedDate$</DATE>

  <SHORT_DESCRIPTION name="relocate_handle">Move handles inside the graphic
  hierarchy.</SHORT_DESCRIPTION>

  <CALLING_SEQUENCE>
    <CALLING_SEQUENCE_ITEM>relocate_handle( movedHandles, parent
    )</CALLING_SEQUENCE_ITEM>
  </CALLING_SEQUENCE>

  <PARAM>
    <PARAM_INDENT>
      <PARAM_ITEM>
        <PARAM_NAME>movedHandles</PARAM_NAME>

        <PARAM_DESCRIPTION>
          <SP>: Vector of relocated handles.</SP>
        </PARAM_DESCRIPTION>
      </PARAM_ITEM>

      <PARAM_ITEM>
        <PARAM_NAME>parent</PARAM_NAME>

        <PARAM_DESCRIPTION>
          <SP>: New parent of the handles.</SP>
        </PARAM_DESCRIPTION>
      </PARAM_ITEM>
    </PARAM_INDENT>
  </PARAM>

  <DESCRIPTION>
    <P>The relocate_handle function allows to move handles from their current
    locations in the graphical hierarchy to another. All the moved entities
    are relocated under the same parent handle specified with the
    <BD>parent</BD> parameter.</P>

    <P>Since not every handles are compatible with each others, some
    restrictions exist when relocationg handles. For examples, it is not
    allowed to move an axes handle under a polyline. More information about
    compatibility can be found in the <LINK>graphics_entities</LINK> page.</P>

    <P>This routine is particularly useful to move an object from an axes
    entity to an other or to move axes from figures handles.</P>
  </DESCRIPTION>

  <EXAMPLE><![CDATA[x = 0:10 ;

// plot a first polyline
plot(x,x^2) ;
axes1 = gca() ;
poly1 = gce() ;

// plot a second in an other window
scf() ;
plot( x,x )  ;
axes2 = gca() ;
poly2 = gce()  ;
poly2bis = copy( poly2 ) ; // make a copy of the polyline

// put both polyline in the same window
relocate_handle( poly2bis, axes1 ) ;

]]></EXAMPLE>

  <SEE_ALSO>
    <SEE_ALSO_ITEM>
      <LINK>graphics_entities</LINK>
    </SEE_ALSO_ITEM>

    <SEE_ALSO_ITEM>
      <LINK>copy</LINK>
    </SEE_ALSO_ITEM>

    <SEE_ALSO_ITEM>
      <LINK>delete</LINK>
    </SEE_ALSO_ITEM>

    <SEE_ALSO_ITEM>
      <LINK>swap_handles</LINK>
    </SEE_ALSO_ITEM>
  </SEE_ALSO>

  <AUTHOR>Jean-Baptiste Silvy</AUTHOR>
</MAN>
