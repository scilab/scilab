<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE MAN SYSTEM "../../../../modules/helptools/help.dtd">
<MAN>
  <LANGUAGE>eng</LANGUAGE>

  <TITLE>grep</TITLE>

  <TYPE>Scilab Function</TYPE>

  <DATE>$LastChangedDate: 2008-01-07 16:57:13 +0200 (Mon, 07 Jan 2008)$</DATE>

  <SHORT_DESCRIPTION name="grep">find matches of a string in a vector of
  strings</SHORT_DESCRIPTION>

  <CALLING_SEQUENCE>
    <CALLING_SEQUENCE_ITEM>row=grep(haystack,needle )</CALLING_SEQUENCE_ITEM>

    <CALLING_SEQUENCE_ITEM>[row,which]=grep(haystack,needle
    )</CALLING_SEQUENCE_ITEM>

    <CALLING_SEQUENCE_ITEM>row=grep(haystack,needle
    ,["flag"])</CALLING_SEQUENCE_ITEM>

    <CALLING_SEQUENCE_ITEM>[row,which]=grep(haystack,needle
    ,["flag"])</CALLING_SEQUENCE_ITEM>
  </CALLING_SEQUENCE>

  <PARAM>
    <PARAM_INDENT>
      <PARAM_ITEM>
        <PARAM_NAME>haystack</PARAM_NAME>

        <PARAM_DESCRIPTION>
          <SP>: a vector of strings.</SP>
        </PARAM_DESCRIPTION>
      </PARAM_ITEM>

      <PARAM_ITEM>
        <PARAM_NAME>needle </PARAM_NAME>

        <PARAM_DESCRIPTION>
          <SP>: a character string or character string vector . The string(s)
          to search in <VERB>haystack</VERB></SP>
        </PARAM_DESCRIPTION>
      </PARAM_ITEM>

      <PARAM_ITEM>
        <PARAM_NAME>row</PARAM_NAME>

        <PARAM_DESCRIPTION>
          <SP>: vector of indices: row where a match has been found or an
          empty matrix if no match found.</SP>
        </PARAM_DESCRIPTION>
      </PARAM_ITEM>

      <PARAM_ITEM>
        <PARAM_NAME>which</PARAM_NAME>

        <PARAM_DESCRIPTION>
          <SP>: vector of indices: index of needle string found or an empty
          matrix if no match found.</SP>
        </PARAM_DESCRIPTION>
      </PARAM_ITEM>

      <PARAM_ITEM>
        <PARAM_NAME>"Flag"</PARAM_NAME>

        <PARAM_DESCRIPTION>
          <SP>:string("r" for regular expression)</SP>
        </PARAM_DESCRIPTION>
      </PARAM_ITEM>
    </PARAM_INDENT>
  </PARAM>

  <DESCRIPTION>
    <P>Foreach entry of haystack , grep searches if at least a string in
    needle matches a substring. haystack entries index where at least a match
    has been found are returned in the row argument. while optionnal which
    argument gives the index of first string of needle found. When using the
    third parameters "r", the needle should be a string of regular expression.
    And then grep is going to match it with haystack according to the regular
    express rules.</P>
  </DESCRIPTION>

  <EXAMPLE><![CDATA[

txt=['find matches of a string in a vector of strings'
     'search position of a character string in an other string'
     'Compare Strings'];

grep(txt,'strings')
grep(txt,['strings' 'Strings'])

[r,w]=grep(txt,['strings' 'Strings'])

str = ["hat";"cat";"hhat";"chat";"hcat";"ccchat";"at";"dog"]

grep(str,'/[hc]+at/','r')
grep(str,'/[hc]?at/','r')
grep(str,'/cat|dog/','r')

  ]]></EXAMPLE>

  <SEE_ALSO>
    <SEE_ALSO_ITEM>
      <LINK>strindex</LINK>
    </SEE_ALSO_ITEM>
  </SEE_ALSO>
</MAN>