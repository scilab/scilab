<?xml version="1.0" encoding="ISO-8859-1"?>
<!--
 * Scilab ( http://www.scilab.org/ ) - This file is part of Scilab
 * Copyright (C) 1997-2008 - INRIA
 * 
 * This file must be used under the terms of the CeCILL.
 * This source file is licensed as described in the file COPYING, which
 * you should have received as part of this distribution.  The terms
 * are also available at    
 * http://www.cecill.info/licences/Licence_CeCILL_V2-en.txt
 *
 -->
<refentry xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:svg="http://www.w3.org/2000/svg" xmlns:mml="http://www.w3.org/1998/Math/MathML" xmlns:db="http://docbook.org/ns/docbook" version="5.0-subset Scilab" xml:lang="en" xml:id="pvm_probe">
  <info>
    <pubdate>$LastChangedDate$</pubdate>
  </info>
  <refnamediv>
    <refname>pvm_probe</refname>
    <refpurpose> Check if message has arrived.  </refpurpose>
  </refnamediv>
  <refsynopsisdiv>
    <title>Calling Sequence</title>
    <synopsis>[buffid] = pvm_probe(tid, msgtag)</synopsis>
  </refsynopsisdiv>
  <refsection>
    <title>Parameters</title>
    <variablelist>
      <varlistentry>
        <term>tid</term>
        <listitem>
          <para>integer, task identifier of sending process supplied by the user.</para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>msgtag : integer, message tag supplied by the user.  msgtag should be</term>
        <listitem>
          <para>&gt;= 0.</para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term>buffid</term>
        <listitem>
          <para>integer, returning  the  value  of  the  new  active              receive  buffer  identifier.   Values less than zero              indicate an error.</para>
        </listitem>
      </varlistentry>
    </variablelist>
  </refsection>
  <refsection>
    <title>Description</title>
    <para><literal>pvm_probe</literal> 
    checks to see if a message with  label
         <literal>msgtag</literal>   has  arrived  from <literal>tid</literal>.
         If  a matching message has
         arrived <literal>pvm_probe</literal> returns a buffer identifier in 
         <literal>bufid</literal>. This
         bufid  can be used in a pvm_bufinfo call to determine infor-
         mation about the message such as its source and length.</para>
    <para>
         If the requested message has  not  arrived,  then  pvm_probe
         returns  with  a 0 in bufid. If some error occurs bufid will
         be &lt; 0.</para>
    <para>
         A -1 in msgtag or tid matches  anything.   This  allows  the
         user  the  following  options.   If  tid  = -1 and msgtag is
         defined by the user, then pvm_probe will  accept  a  message
         from  any  process which has a matching msgtag.  If msgtag =
         -1 and tid is defined  by  the  user,  then  pvm_probe  will
         accept  any message that is sent from process tid.  If tid =
         -1 and msgtag = -1, then pvm_probe will accept  any  message
         from any process.</para>
    <para>
         pvm_probe can be called multiple times to check if  a  given
         message  has  arrived  yet.  After  the message has arrived,
         pvm_recv must be called before the message can  be  unpacked
         into the user's memory using the unpack routines.</para>
    <para>
    For example</para>
    <programlisting><![CDATA[

arrived = pvm_probe( tid, msgtag );
if (arrived >= 0) then [bytes, msgtag, tid, info] = pvm_info(arrived); end
   
    ]]></programlisting>
  </refsection>
  <refsection>
    <title>See Also</title>
    <simplelist type="inline">
      <member>
        <link linkend="pvm_recv">pvm_recv</link>
      </member>
    </simplelist>
  </refsection>
</refentry>
